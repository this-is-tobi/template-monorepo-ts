apiVersion: v1
kind: Secret
metadata:
  name: {{ include "template.api.fullname" . }}
  labels:
    {{- include "template.api.labels" . | nindent 4 }}
data:
  {{- include "template.containerSecret" .Values.api.container | nindent 2 }}
