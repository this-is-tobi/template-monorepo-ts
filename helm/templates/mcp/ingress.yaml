{{- if and .Values.mcp.enabled .Values.mcp.ingress.enabled -}}
{{- $fullNameApp := printf "%s-%s" (include "helper.fullname" .) "mcp" -}}
{{- $svcPortApp := .Values.mcp.service.port -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "mcp" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "mcp")) | nindent 4 }}
  {{- if .Values.mcp.ingress.labels }}
    {{- toYaml .Values.mcp.ingress.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.mcp.ingress.annotations }}
  annotations:
    {{- tpl (toYaml .Values.mcp.ingress.annotations) . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.mcp.ingress.className }}
  ingressClassName: {{ .Values.mcp.ingress.className }}
  {{- end }}
  {{- if gt (len .Values.mcp.ingress.tls) 0 }}
  tls:
  {{- range .Values.mcp.ingress.tls }}
  - hosts:
    {{- range .hosts }}
    - {{ . | quote }}
    {{- end }}
    secretName: {{ .secretName | quote | default (printf "%s-%s" $fullNameApp "tls") }}
  {{- end }}
  {{- end }}
  rules:
  {{- range .Values.mcp.ingress.hosts }}
  - host: {{ .name | quote }}
    http:
      paths:
      - path: {{ .path | default "/" }}
        pathType: {{ .pathType | default "Prefix" }}
        backend:
          service:
          {{- if .backend }}
            name: {{ .backend.serviceName | default $fullNameApp }}
            port:
              number: {{ .backend.portNumber | default $svcPortApp }}
          {{- else }}
            name: {{ $fullNameApp }}
            port:
              number: 80
          {{- end }}
  {{- end }}
{{- end }}
