{{- if and .Values.mcp.enabled .Values.mcp.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s-%s" (include "helper.fullname" .) "mcp" "metrics" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "mcp")) | nindent 4 }}
  {{- if .Values.mcp.metrics.service.labels }}
    {{- toYaml .Values.mcp.metrics.service.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.mcp.metrics.service.annotations }}
  annotations: {{- toYaml .Values.mcp.metrics.service.annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.mcp.metrics.service.type }}
  ports:
  - port: {{ .Values.mcp.metrics.service.port }}
    targetPort: {{ .Values.mcp.metrics.service.targetPort }}
    protocol: TCP
  selector: {{- include "helper.selectorLabels" (dict "root" . "componentName" "mcp") | nindent 4 }}
{{- end }}
