{{- if and .Values.mcp.enabled .Values.mcp.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "mcp" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "mcp")) | nindent 4 }}
  {{- if .Values.mcp.pdb.labels }}
    {{- toYaml .Values.mcp.pdb.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.mcp.pdb.annotations }}
  annotations: {{- toYaml .Values.mcp.pdb.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.mcp.pdb.maxUnavailable }}
  maxUnavailable: {{ . }}
  {{- else }}
  minAvailable: {{ .Values.mcp.pdb.minAvailable | default 0 }}
  {{- end }}
  selector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "mcp") | nindent 6 }}
{{- end }}
