{{- if and .Values.mcp.enabled .Values.mcp.serviceAccount.enabled .Values.mcp.serviceAccount.clusterRole.create .Values.mcp.serviceAccount.clusterRole.rules }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "mcp" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "mcp")) | nindent 4 }}
rules: {{- toYaml .Values.mcp.serviceAccount.clusterRole.rules | nindent 0 }}
{{- end }}
