{{- if and .Values.mcp.enabled .Values.mcp.networkPolicy.create }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "mcp" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "mcp")) | nindent 4 }}
  {{- if .Values.mcp.pdb.labels }}
    {{- toYaml .Values.mcp.pdb.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.mcp.pdb.annotations }}
  annotations: {{- toYaml .Values.mcp.pdb.annotations | nindent 4 }}
  {{- end }}
spec:
  policyTypes: {{- toYaml .Values.mcp.networkPolicy.policyTypes | nindent 2 }}
  {{- with .Values.mcp.networkPolicy.ingress }}
  ingress: {{- toYaml . | nindent 2 }}
  {{- end }}
  {{- with .Values.mcp.networkPolicy.egress }}
  egress: {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
