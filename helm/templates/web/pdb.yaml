{{- if .Values.web.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "web" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "web")) | nindent 4 }}
  {{- if .Values.web.pdb.labels }}
    {{- toYaml .Values.web.pdb.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.web.pdb.annotations }}
  annotations: {{- toYaml .Values.web.pdb.annotations | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.web.pdb.maxUnavailable }}
  maxUnavailable: {{ . }}
  {{- else }}
  minAvailable: {{ .Values.web.pdb.minAvailable | default 0 }}
  {{- end }}
  selector:
    matchLabels: {{- include "helper.selectorLabels" (dict "root" . "componentName" "web") | nindent 6 }}
{{- end }}
