apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "web" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "web")) | nindent 4 }}
spec:
  type: {{ .Values.web.service.type | default "ClusterIP" }}
  ports:
  - port: {{ .Values.web.service.port | default 80 }}
    targetPort: {{ .Values.web.containerPortName | default .Values.web.containerPort | default 8080 }}
    protocol: {{ .Values.web.service.protocol | default "TCP" }}
    {{- if eq .Values.web.service.type "NodePort" }}
    nodePort: {{ .Values.web.service.nodePort }}
    {{- end }}
    name: {{ .Values.web.service.portName | default "http" }}
  {{- if .Values.web.service.extraPorts -}}
  {{- toYaml .Values.web.service.extraPorts | nindent 2 -}}
  {{- end }}
  selector: {{- include "helper.selectorLabels" (dict "root" . "componentName" "web") | nindent 4 }}
