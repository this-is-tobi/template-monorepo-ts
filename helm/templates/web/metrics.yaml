{{- if .Values.web.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s-%s" (include "helper.fullname" .) "web" "metrics" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "web")) | nindent 4 }}
  {{- if .Values.web.metrics.service.labels }}
    {{- toYaml .Values.web.metrics.service.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.web.metrics.service.annotations }}
  annotations: {{- toYaml .Values.web.metrics.service.annotations | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.web.metrics.service.type }}
  ports:
  - port: {{ .Values.web.metrics.service.port }}
    targetPort: {{ .Values.web.metrics.service.targetPort }}
    protocol: TCP
  selector: {{- include "helper.selectorLabels" (dict "root" . "componentName" "web") | nindent 4 }}
{{- end }}
