{{- if and .Values.web.serviceAccount.enabled .Values.web.serviceAccount.role.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "web" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "web")) | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ printf "%s-%s" (include "helper.fullname" .) "web" }}
subjects:
- kind: ServiceAccount
  name: {{ .Values.web.serviceAccount.name | default (printf "%s-%s" (include "helper.fullname" .) "web") }}
  namespace: {{ .Release.Namespace }}
{{- end }}
