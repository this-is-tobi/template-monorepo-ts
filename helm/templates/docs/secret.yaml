{{- if or .Values.docs.envSecret .Values.global.envSecret -}}
kind: Secret
apiVersion: v1
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "docs" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "docs")) | nindent 4 }}
data:
  {{- if .Values.global.envSecret -}}
  {{- include "helper.secret" .Values.global.envSecret | indent 2 }}
  {{- end -}}
  {{- if .Values.docs.envSecret -}}
  {{- include "helper.secret" .Values.docs.envSecret | indent 2 }}
  {{- end -}}
{{- end -}}
