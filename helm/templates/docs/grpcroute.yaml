{{- if .Values.docs.grpcRoute.enabled -}}
{{- $fullNameApp := printf "%s-%s" (include "helper.fullname" .) "docs" -}}
{{- $svcPortApp := .Values.docs.service.port -}}
apiVersion: gateway.networking.k8s.io/v1
kind: GRPCRoute
metadata:
  name: {{ printf "%s-%s" (include "helper.fullname" .) "docs" }}
  labels: {{- (include "helper.labels" (dict "root" . "componentName" "docs")) | nindent 4 }}
  {{- if .Values.docs.grpcRoute.labels }}
    {{- toYaml .Values.docs.grpcRoute.labels | nindent 4 }}
  {{- end }}
  {{- if .Values.docs.grpcRoute.annotations }}
  annotations:
    {{- tpl (toYaml .Values.docs.grpcRoute.annotations) . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.docs.grpcRoute.parentRefs }}
  parentRefs:
  {{- range .Values.docs.grpcRoute.parentRefs }}
  - name: {{ .name }}
    {{- if .namespace }}
    namespace: {{ .namespace }}
    {{- end }}
    {{- if .sectionName }}
    sectionName: {{ .sectionName }}
    {{- end }}
    {{- if .port }}
    port: {{ .port }}
    {{- end }}
    {{- if .group }}
    group: {{ .group }}
    {{- end }}
    {{- if .kind }}
    kind: {{ .kind }}
    {{- end }}
  {{- end }}
  {{- end }}
  {{- if .Values.docs.grpcRoute.hostnames }}
  hostnames:
  {{- range .Values.docs.grpcRoute.hostnames }}
  - {{ . | quote }}
  {{- end }}
  {{- end }}
  {{- if .Values.docs.grpcRoute.rules }}
  rules:
  {{- range .Values.docs.grpcRoute.rules }}
  {{- if .matches }}
  - matches:
    {{- range .matches }}
    {{- if .method }}
    - method:
        {{- if .method.type }}
        type: {{ .method.type }}
        {{- end }}
        {{- if .method.service }}
        service: {{ .method.service }}
        {{- end }}
        {{- if .method.method }}
        method: {{ .method.method }}
        {{- end }}
    {{- end }}
      {{- if .headers }}
      headers:
      {{- range .headers }}
      - type: {{ .type | default "Exact" }}
        name: {{ .name }}
        value: {{ .value }}
      {{- end }}
      {{- end }}
    {{- end }}
    {{- end }}
    {{- if .filters }}
    filters:
    {{- toYaml .filters | nindent 4 }}
    {{- end }}
    {{- if .backendRefs }}
    backendRefs:
    {{- range .backendRefs }}
    - name: {{ .name | default $fullNameApp }}
      port: {{ .port | default $svcPortApp }}
      {{- if .weight }}
      weight: {{ .weight }}
      {{- end }}
      {{- if .group }}
      group: {{ .group }}
      {{- end }}
      {{- if .kind }}
      kind: {{ .kind }}
      {{- end }}
      {{- if .namespace }}
      namespace: {{ .namespace }}
      {{- end }}
    {{- end }}
    {{- else }}
    backendRefs:
    - name: {{ $fullNameApp }}
      port: {{ $svcPortApp }}
    {{- end }}
  {{- end }}
  {{- else }}
  {{/* Default rule when no rules specified - routes all gRPC traffic */}}
  rules:
  - backendRefs:
    - name: {{ $fullNameApp }}
      port: {{ $svcPortApp }}
  {{- end }}
{{- end }}
