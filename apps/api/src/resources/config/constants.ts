/**
 * Centralised response messages for the config resource.
 */
export const configMessages = {
  retrieved: 'config successfully retrieved',
  updated: 'config successfully updated',
  forbidden: 'admin role required to update config',
} as const
