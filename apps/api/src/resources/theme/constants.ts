/**
 * Centralised response messages for the theme resource.
 */
export const themeMessages = {
  retrieved: 'theme successfully retrieved',
  updated: 'theme successfully updated',
  forbidden: 'admin role required to update theme',
} as const
