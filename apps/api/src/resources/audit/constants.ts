/**
 * Centralised response messages for the audit resource.
 */
export const auditMessages = {
  retrieved: 'audit logs successfully retrieved',
  unavailable: 'audit module is not enabled',
} as const
