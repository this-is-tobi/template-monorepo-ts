datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output = "../src/generated/prisma"
  engineType = "client"
}

model Users {
  id          String  @id @unique @default(uuid())
  firstname   String
  lastname    String
  email       String
  bio         String?
}
